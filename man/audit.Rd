% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/audit.R
\name{audit}
\alias{audit}
\title{Summaries of scrutiny objects}
\usage{
audit(data)
}
\arguments{
\item{data}{A data frame with one of the classes named above.}
}
\value{
A tibble (data frame) with test summary statistics.
}
\description{
\code{audit()} is an S3 generic to follow up on those scrutiny
functions that perform tests on a data frame. It summarizes results of
those tests and presents the summaries in a tibble.

Below is a list of functions that return objects with classes for which
there are \code{audit()} methods. This means you can run \code{audit()} on the output
returned by any of these functions.

Go to the documentation of any function below to learn about its particular
\code{audit()} method.\tabular{ll}{
   \strong{Function} \tab \strong{Class} \cr
   \code{grim_map()} \tab \code{scr_grim_map} \cr
   \code{grim_map_disperse()} \tab \code{scr_grim_map_disperse} \cr
   \code{debit_map()} \tab \code{scr_debit_map} \cr
   \code{debit_map_disperse()} \tab \code{scr_debit_map_disperse} \cr
   \code{duplicate_count()} \tab \code{scr_dup_count} \cr
   \code{duplicate_detect()} \tab \code{scr_dup_detect} \cr
}
}
\examples{
# For GRIM-testing:
pigs1 \%>\%
  grim_map() \%>\%
  audit()

# For detecting duplicates:
pigs4 \%>\%
  duplicate_detect() \%>\%
  audit()
}
