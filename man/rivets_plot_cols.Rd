% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rivets-plot-cols.R
\name{rivets_plot_cols}
\alias{rivets_plot_cols}
\title{Visualize RIVETS using columns}
\usage{
rivets_plot_cols(
  values_1,
  values_2,
  dir,
  label_x_axis = NULL,
  label_y_axis = "Test statistic",
  text_size = 12,
  color_1 = "royalblue1",
  color_2 = "red",
  alpha_1 = 0.5,
  alpha_2 = 0.5,
  tag = ggplot2::waiver()
)
}
\arguments{
\item{values_1, values_2}{Numeric or string coercible to numeric (length 3).
Each vector consists of one reported statistic plus the lower and upper
bounds of the original number, as determined by \code{unround()}.}

\item{dir}{String (length 1). Direction on the x-axis in which the test
statistic increases. Either \code{"forward"}, \code{"backward"}, \code{"inward"}, or
\code{"outward"}.}

\item{label_x_axis}{String (length 2). X-axis labels. They should signify the
summary statistic.}

\item{label_y_axis}{String (length 1). Y-axis label. It should signify the
test statistic. Default is \code{"Test statistic"}.}

\item{text_size}{Numeric. Base text size. Default is \code{12}.}

\item{color_1, color_2}{String. Line colors. Defaults are \code{"royalblue1"} and
\code{"red"}.}

\item{tag}{Tag for the whole plot. Default is \code{waiver()}, i.e., no tag.}
}
\value{

}
\description{
\code{rivets_plot_cols()} displays the relationship between the rounding bounds of
a reported summary statistic (such as means or standard deviations) and a
corresponding test statistic (such as \emph{t}).
}
\examples{
# In the t-test context, a pair of means
# along with their rounding bounds:
rivets_plot_cols(
  values_1 = c(4.525, 4.53, 4.535),
  values_2 = c(3.735, 3.73, 3.725),
  dir = "outward",
  label_x_axis = c("Mean 2", "Mean 1"),
  label_y_axis = "Test statistic (t)",
  tag = "a"
)

# Also in the t-test context,
# the same for standard deviations:
rivets_plot_cols(
  values_1 = c(1.065, 1.07, 1.075),
  values_2 = c(0.925, 0.92, 0.915),
  dir = "backward",
  label_x_axis = c("SD 2", "SD 1"),
  label_y_axis = "Test statistic (t)",
  tag = "b"
)
}
\references{
Brown, N. J. L., & Heathers, J. (2019). Rounded Input Variables,
Exact Test Statistics (RIVETS). PsyArXiv.
https://doi.org/10.31234/osf.io/ctu9z
}
